class Url < ActiveRecord::Base
  # attr_accessor :long_url, :short_url
  # attr_accessible (rails 3 only?)
  # since this is active record, attributes come from db columns? so do we do a db_migrate
end
